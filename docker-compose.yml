services:
  birthday-web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: birthday-etin-app
    restart: always
    ports:
      - "3000:3000"
    volumes:
      - ./dist:/app/dist
      - ./public:/app/public

